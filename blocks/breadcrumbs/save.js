/**
 * Save function for the breadcrumbs block
 */
const BreadcrumbsSave = () => {
    // Since we're using server-side rendering,
    // we don't need to render anything here
    return null;
};

export default BreadcrumbsSave;