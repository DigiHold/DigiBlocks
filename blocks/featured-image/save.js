/**
 * Save function for the featured image block
 */
const FeaturedImageSave = () => {
    // Since we're using server-side rendering,
    // we don't need to render anything here
    return null;
};

export default FeaturedImageSave;