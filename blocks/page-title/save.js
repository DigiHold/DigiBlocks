/**
 * Save function for the page title block
 */
const PageTitleSave = () => {
    // Since we're using server-side rendering,
    // we don't need to render anything here
    return null;
};

export default PageTitleSave;