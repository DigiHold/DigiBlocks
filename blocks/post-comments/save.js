/**
 * Save function for the Post Comments block
 */
const PostCommentsSave = () => {
    // Since we're using server-side rendering,
    // we don't need to render anything here
    return null;
};

export default PostCommentsSave;