/**
 * Post Content Block Save function
 */
const PostContentSave = () => {
    // Since we're using server-side rendering,
    // we don't need to render anything here
    return null;
};

export default PostContentSave;