/**
 * Save function for the Taxonomy block
 */
const TaxonomySave = () => {
    // Since we're using server-side rendering,
    // we don't need to render anything here
    return null;
};

export default TaxonomySave;